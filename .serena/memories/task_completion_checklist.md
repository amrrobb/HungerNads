# Task Completion Checklist

1. Run `npm run typecheck` to verify TypeScript compiles
2. Check that imports match actual exports of dependent modules
3. Follow existing code style: file-level JSDoc, section separators, pure functions
4. Do NOT commit unless explicitly asked
5. Close beads task with `bd close <task-id>` when done
